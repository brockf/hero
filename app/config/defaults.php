<?php  if ( ! defined('BASEPATH')) exit('No direct script access allowed');

/**
* Default Configurations
*
* These configurations were added post-release.  They are specified here because they are
* default and very unlikely to be changed at the user's site.
*/

$config['allow_get_array'] = TRUE;